package GUI;

import javax.swing.*;

public class StatsPanel extends JFrame {

    private JPanel statsPanel;

    public StatsPanel(){
        add(statsPanel);
        setTitle("Stats");
        setSize(600, 300);
        setVisible(true);
    }
}
